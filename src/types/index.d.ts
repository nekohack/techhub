export type SiteMetadataTypes = {
    data: {
        title: string
        titleTemplate: string
        description: string
        author: string
        locale: string
        siteUrl: string
        image: string
    }
}
